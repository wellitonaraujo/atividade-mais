import React from 'react';
import * as S from './styles';
interface ActivityInfosProps {
    activityType?: string;
}

const StartControl: React.FC<ActivityInfosProps> = ({}) => {

  return (
    <S.Container>
    
    </S.Container>
  );
};

export default StartControl;
